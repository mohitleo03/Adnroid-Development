<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kanika.sehrawat.bakeryapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Bluetooth permission -->
    <uses-permission android:name="android.permission.BLUETOOTH" /> <!-- Location permission. This permission is sensitive and needs to be dynamically applied for in the code after being declared. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Location permission. If only the IP address-based time capture function is used, you can declare only this permission. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- Allow the app to read common data. -->
    <uses-permission android:name="com.huawei.appmarket.service.commondata.permission.GET_COMMON_DATA" /> <!-- Allow the app to change the WLAN status. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BakeryApp">
        <activity android:name=".Activity.CategoryActivity"></activity>
        <activity android:name=".Activity.ContactActivity" />
        <activity
            android:name=".Activity.FoodActivity"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity android:name=".Activity.CartListActivity" />
        <activity android:name=".Activity.ShowDetailsActivity" />
        <activity android:name=".Activity.ProfileActivity" />
        <activity android:name=".Activity.LocationActivity" />
        <activity android:name=".Activity.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>